
  src\Entitlements.jsx:260:        today.setHours(0, 0, 0, 0);
  src\Entitlements.jsx:261:        dueDate.setHours(0, 0, 0, 0);
  src\Entitlements.jsx:262:        // يوم الاستحقاق نفسه يُعتبر مستحقاً (متأخر) 
وليس "غير مستحق بعد"
  src\Entitlements.jsx:263:        return { status: dueDate <= today ? "overdue" 
: "not_due", paidState };
  src\Entitlements.jsx:264:      }
  src\Entitlements.jsx:265:    }
  src\Entitlements.jsx:266:    return { status: "overdue", paidState }; // 
احتياطي إذا تعذر حساب التاريخ
  src\Entitlements.jsx:267:  }
  src\Entitlements.jsx:268:
  src\Entitlements.jsx:269:  function statusToArabic(status, paidState) {
  src\Entitlements.jsx:270:    if (status === "paid") return "مدفوع";
  src\Entitlements.jsx:271:    if (status === "not_due") return "غير مستحق بعد";
  src\Entitlements.jsx:272:    // overdue
  src\Entitlements.jsx:273:    return paidState === "partial" ? "متأخر - متبقي 
جزء" : "متأخر";
  src\Entitlements.jsx:274:  }
  src\Entitlements.jsx:275:
  src\Entitlements.jsx:276:  // هل الضريبة تسري على هذه الدفعة، حسب إعداد العقد 
وتاريخ استحقاق الدفعة
  src\Entitlements.jsx:277:  function isTaxApplicable(lease, dueDateHijri) {
  src\Entitlements.jsx:278:    if (!lease?.tax_enabled) return false;
  src\Entitlements.jsx:279:    if (!dueDateHijri || dueDateHijri === "—") return 
false;
  src\Entitlements.jsx:280:    if (!lease.tax_effective_hijri) return true;
  src\Entitlements.jsx:281:    return hijriSortKey(dueDateHijri) >= 
hijriSortKey(lease.tax_effective_hijri);
  src\Entitlements.jsx:282:  }
  src\Entitlements.jsx:283:
  src\Entitlements.jsx:284:  function handleSearch() {
  src\Entitlements.jsx:285:    setShowPropDropdown(false);
  src\Entitlements.jsx:286:    setShowTenantDropdown(false);
  src\Entitlements.jsx:287:    setStatusFilter("all"); // إعادة ضبط فلتر الحالة 
عند كل بحث جديد
  src\Entitlements.jsx:288:    const filterYear = parseInt(selectedYear);
  src\Entitlements.jsx:289:    const filterMonth = parseInt(selectedMonthNum);
> src\Entitlements.jsx:290:    const found = [];
  src\Entitlements.jsx:291:
  src\Entitlements.jsx:292:    for (const row of payments) {
  src\Entitlements.jsx:293:      const lease = row.leases;
  src\Entitlements.jsx:294:      if (selectedProperties.length > 0 && 
!selectedProperties.includes(lease.property_id)) continue;
  src\Entitlements.jsx:295:      if (selectedTenants.length > 0 && 
!selectedTenants.includes(lease.tenants?.name)) continue;


